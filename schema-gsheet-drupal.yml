resource_title: "{{ record['Name of group, organization or person providing this service'] or ''}}"
resource_location__locality: "{{ record['State/Territory'] or ''}}"
resource_description: "{{ record['Briefly describe the offer or request'] or ''}}"
contact_phone: "{{ record['Service Telephone Number (Optional)'] or ''}}"
contact_email: "{{ record['Service Email (Optional)'] or ''}}"
resource_location__value: "{{ record['Service Address (optional)'] or ''}}"
link: "{{ record['Link (optional)'] or ''}}"
resource_location__lat: "{{ record['Latitude'] or ''}}"
resource_location__lng: "{{ record['Longitude'] or ''}}"
notes: "Imported from Google Sheets {{ date }}"
entity_type: "webform"
entity_id: "resource_intake"
public_contact: "1"
langcode: "en"
resource_category: "{{ category_ids | join(',') }}"
contact_name: "{{ record['Name of group, organization or person providing this service'] or ''}}"
