title: "{{ record['resource_title'] or ''}}"
category: "{{ record['category'] or ''}}"
category_1: "{{ record['category_sub'] or ''}}"
category_2: "{{ record['category_sub_sub'] or ''}}"
location: "{{ record['resource_location']['locality'] }}"
description: "{{ record['resource_description'] or ''}}"
phone: "{{ record['contact_phone'] or ''}}"
email: "{{ record['contact_email'] or ''}}"
address: "{{ record['resource_location']['formatted_address'] or ''}}"
link: "{{ record['link'] or ''}}"
lat: "{{ record['resource_location']['lat'] or ''}}"
long: "{{ record['resource_location']['lng'] or ''}}"
uuid: "{{ record['uuid'] or ''}}"
